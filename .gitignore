# See https://help.github.com/articles/ignoring-files/ for more about ignoring files.

# dependencies
/node_modules
/.pnp
.pnp.js

# testing
/coverage

# next.js
/.next/
/out/

# env (do not commit secrets)
.env
.env.local
.env.*.local

# production
/build

# misc
.DS_Store
*.pem

# debug
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# local env files
.env*.local

# vercel
.vercel

# typescript
*.tsbuildinfo
next-env.d.ts

# data: 로컬 작업 후 배포 시 content.json, journals.xlsx를 Git에 포함하려면 아래 두 줄을 주석 처리하세요.
# (Redis/Blob 없이 배포할 때 배포본이 이 파일들을 읽어서 표시합니다)
# data/*.json
# data/*.xlsx
!data/.gitkeep

# public/uploads: 업로드 사진은 Git에 포함해 NAS·Vercel 모두 반영 (이 디렉터리는 무시하지 않음)

# local backups (optional: remove this block if you want to track backups)
backup_*/




