export const metadata = {
  title: "Conference Presentations | Achievements",
  description: "Conference presentations and proceedings",
};

export default function ConferenceLayout({
  children,
}: {
  children: React.ReactNode;
}) {
  return <>{children}</>;
}

